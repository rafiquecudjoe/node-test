export class Queues {
    static emailsQueue = {
        name: 'EMAILS QUEUE',
        description: 'This is the queue for sending emails',
    };
}

export class QueuesProcessors {
    static emailsQueueProcessor = {
        name: 'EMAILS QUEUE PROCESSOR',
        description: 'This is the processor for sending emails',
    };
}
